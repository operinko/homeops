---
# Configuration for crowdsec application
# Used by ApplicationSet generator
app:
  name: crowdsec
  chartRepo: "https://crowdsecurity.github.io/helm-charts"
  chartName: "crowdsec"
  chartVersion: "0.20.1"
  enabled: true
