---
apiVersion: v1
kind: ConfigMap
metadata:
  name: technitium-gatus-ep
  namespace: network
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "technitium"
        group: guarded
        url: 1.1.1.1
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        dns:
          query-name: "technitium.vaderrp.com"
          query-type: A
        conditions:
          - "len([BODY]) == 0"