---
# Patch to add subdomain to pod spec for FQDN resolution
# This enables dns1.vaderrp-com.network.svc.cluster.local
apiVersion: apps/v1
kind: Deployment
metadata:
  name: technitium
  namespace: network
spec:
  template:
    spec:
      subdomain: vaderrp-com

