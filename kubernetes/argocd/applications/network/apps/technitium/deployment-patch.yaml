---
# Patch to add subdomain to pod spec for FQDN resolution
# This enables ns1.dns-vaderrp-com.network.svc.cluster.local
apiVersion: apps/v1
kind: Deployment
metadata:
  name: technitium
  namespace: network
spec:
  template:
    spec:
      subdomain: dns-vaderrp-com

