---
# Configuration for internal-dns application
# Used by ApplicationSet generator
app:
  name: internal-dns
  chartRepo: "https://kubernetes-sigs.github.io/external-dns"
  chartName: "external-dns"
  chartVersion: "1.19.0"
  enabled: true
