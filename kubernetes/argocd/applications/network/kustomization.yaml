---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # Wave 0 - Namespace
  - network-namespace.yaml
  # ApplicationSets manage: cloudflare-dns, cloudflare-tunnel, crowdsec, internal-dns, technitium
  # See kubernetes/argocd/applicationsets/network.yaml
  # Still managed individually:
  - traefik-resources.yaml
  - traefik.yaml
  - gateways.yaml

