---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gpro-frontend-runner-resources
  namespace: argocd
spec:
  project: actions-runner-system
  source:
    repoURL: https://github.com/operinko/homeops.git
    targetRevision: main
    path: kubernetes/argocd/applications/actions-runner-system/runners/gpro-frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: actions-runner-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jqPathExpressions:
        - '.spec'
        - '.status'

