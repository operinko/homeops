---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-gatus-ep
  namespace: observability
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "gatus (external)"
        group: external
        url: "https://gatus.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"

      - name: "gatus (internal)"
        group: internal
        url: "https://gatus.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://192.168.7.7:53
        conditions:
          - "[STATUS] == 200"