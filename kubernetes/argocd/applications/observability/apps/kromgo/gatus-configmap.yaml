---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kromgo-gatus-ep
  namespace: observability
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "kromgo"
        group: guarded
        url: 1.1.1.1
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        dns:
          query-name: "kromgo.vaderrp.com"
          query-type: A
        conditions:
          - "len([BODY]) == 0"