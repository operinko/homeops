---
# Helm values for goldilocks deployment
# Using default values from chart
containers:
  - name: goldilocks
    resources:
      requests:
        cpu: 601m
        memory: 128Mi
      limits:
        memory: 128Mi
