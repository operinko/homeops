---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # Wave 0: Namespace
  - observability-namespace.yaml
  # ApplicationSets manage: gatus, goldilocks, grafana, kromgo, kube-prometheus-stack, loki, unpoller
  # See kubernetes/argocd/applicationsets/observability.yaml
  # Still managed individually:
  - vpa.yaml
  - syslog-gateway.yaml
  - promtail.yaml

