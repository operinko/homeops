---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: default-namespace
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/operinko/homeops.git
    targetRevision: main
    path: kubernetes/argocd/applications/default/namespace
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: ""
      kind: Namespace
      name: default
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: ""
      kind: Secret
      name: cluster-secrets
      namespace: default
      jsonPointers:
        - /data
    - group: bitwarden.external-secrets.io
      kind: BitwardenSecret
      name: cluster-secrets-default
      jsonPointers:
        - /spec/secrets
    - group: ""
      kind: ConfigMap
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: source.toolkit.fluxcd.io
      kind: OCIRepository
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: traefik.io
      kind: Middleware
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations

