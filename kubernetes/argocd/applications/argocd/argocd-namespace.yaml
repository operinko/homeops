---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-namespace
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: argocd
  source:
    repoURL: https://github.com/operinko/homeops.git
    targetRevision: main
    path: kubernetes/argocd/applications/argocd/namespace
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: ""
      kind: Namespace
      name: argocd
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: ""
      kind: Secret
      name: cluster-secrets
      namespace: argocd
      jsonPointers:
        - /data
    - group: bitwarden.external-secrets.io
      kind: BitwardenSecret
      name: cluster-secrets-argocd
      jsonPointers:
        - /spec/secrets
    - group: ""
      kind: ConfigMap
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: source.toolkit.fluxcd.io
      kind: OCIRepository
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations
    - group: traefik.io
      kind: Middleware
      jsonPointers:
        - /metadata/labels
        - /metadata/annotations

