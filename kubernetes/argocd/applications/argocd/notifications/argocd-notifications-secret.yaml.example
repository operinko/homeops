---
# Example notification secret configuration
# Copy this file to argocd-notifications-secret.yaml and configure your notification service
# Then create an ExternalSecret or SealedSecret to manage it

apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  # Slack example
  slack-token: "xoxb-your-slack-bot-token"
  
  # Discord example (webhook URL)
  # discord-webhook-url: "https://discord.com/api/webhooks/..."
  
  # Email example
  # email-username: "your-email@example.com"
  # email-password: "your-password"
  
  # Microsoft Teams example (webhook URL)
  # teams-webhook-url: "https://outlook.office.com/webhook/..."

---
# Alternative: Use ExternalSecret to pull from Bitwarden
# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: argocd-notifications-secret
#   namespace: argocd
# spec:
#   refreshInterval: 1h
#   target:
#     name: argocd-notifications-secret
#     creationPolicy: Owner
#     deletionPolicy: Retain
#   data:
#     - secretKey: slack-token
#       sourceRef:
#         storeRef:
#           name: bitwarden-fields
#           kind: ClusterSecretStore
#       remoteRef:
#         key: <bitwarden-item-id>
#         property: slack-token

