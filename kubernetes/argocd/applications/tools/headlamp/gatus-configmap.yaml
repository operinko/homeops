---
apiVersion: v1
kind: ConfigMap
metadata:
  name: headlamp-gatus-ep
  namespace: tools
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "headlamp (external)"
        group: external
        url: "https://headlamp.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"

      - name: "headlamp (internal)"
        group: internal
        url: "https://headlamp.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://192.168.7.7:53
        conditions:
          - "[STATUS] == 200"

