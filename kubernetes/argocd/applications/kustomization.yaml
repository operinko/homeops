---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # Helm repositories (required for HelmReleases)
  - ./external-secrets-helmrepos.yaml
  # Namespace infrastructure (must be deployed first)
  - ./default-namespace.yaml
  - ./tools-namespace.yaml
  - ./external-secrets-namespace.yaml
  # Default namespace applications
  - ./echo.yaml
  - ./homepage.yaml
  - ./atuin.yaml
  - ./audiobookshelf.yaml
  # Archived applications (disabled)
  # - ./archive/adventurelog.yaml

