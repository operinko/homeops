---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # Wave 0 - Namespace
  - media-namespace.yaml
  # Applications are now managed by ApplicationSets in kubernetes/argocd/applicationsets/media.yaml
  # The ApplicationSets auto-discover apps from kubernetes/argocd/applications/media/apps/*/config.yaml

