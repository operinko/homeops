---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # Wave 0 - Namespace
  - media-namespace.yaml
  # Per-app ApplicationSets are in kubernetes/argocd/applicationsets/ directory
  # They are applied directly by the root applications Application

