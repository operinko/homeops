---
apiVersion: v1
kind: ConfigMap
metadata:
  name: huntarr-gatus-ep
  namespace: media
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "huntarr"
        group: guarded
        url: 1.1.1.1
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        dns:
          query-name: "huntarr.vaderrp.com"
          query-type: A
        conditions:
          - "len([BODY]) == 0"