---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
  - httproute.yaml
  - migration-job.yaml
  - pvc.yaml
  - pvc-data.yaml
  - replication-destination.yaml
  - replication-source.yaml
  - volsync-secret.yaml
  - vpa.yaml
configMapGenerator:
  - name: readarr-loki-rules
    files:
      - readarr.yaml=./lokirule.yaml
    options:
      labels:
        loki_rule: "true"
generatorOptions:
  disableNameSuffixHash: true
