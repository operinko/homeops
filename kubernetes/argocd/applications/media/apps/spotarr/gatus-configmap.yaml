---
apiVersion: v1
kind: ConfigMap
metadata:
  name: spotarr-gatus-ep
  namespace: media
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "spotarr"
        group: guarded
        url: 1.1.1.1
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        dns:
          query-name: "spotarr.vaderrp.com"
          query-type: A
        conditions:
          - "len([BODY]) == 0"