---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tautulli-gatus-ep
  namespace: media
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "tautulli (external)"
        group: external
        url: "https://tautulli.vaderrp.com/status"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"

      - name: "tautulli (internal)"
        group: internal
        url: "https://tautulli.vaderrp.com/status"
        interval: 1m
        client:
          dns-resolver: tcp://192.168.7.7:53
        conditions:
          - "[STATUS] == 200"