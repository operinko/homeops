---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tvheadend-gatus-ep
  namespace: media
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "tvheadend (external)"
        group: external
        url: "https://tvheadend.vaderrp.com/status"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"

      - name: "tvheadend (internal)"
        group: internal
        url: "https://tvheadend.vaderrp.com/status"
        interval: 1m
        client:
          dns-resolver: tcp://192.168.7.7:53
        conditions:
          - "[STATUS] == 200"