# Policy Reporter values
ui:
  enabled: true
  plugins:
    kyverno: true

# Enable the Kyverno Plugin - required for UI plugin to work
kyvernoPlugin:
  enabled: true

metrics:
  enabled: true
service:
  type: ClusterIP
  port: 8080
