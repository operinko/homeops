---
apiVersion: v1
kind: ConfigMap
metadata:
  name: vaultwarden-gatus-ep
  namespace: security
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: "vaultwarden (external)"
        group: external
        url: "https://vaultwarden.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"

      - name: "vaultwarden (internal)"
        group: internal
        url: "https://vaultwarden.vaderrp.com/"
        interval: 1m
        client:
          dns-resolver: tcp://192.168.7.7:53
        conditions:
          - "[STATUS] == 200"