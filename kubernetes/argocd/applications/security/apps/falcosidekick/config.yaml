---
app:
  name: falcosidekick
  chartRepo: "https://falcosecurity.github.io/charts"
  chartName: "falcosidekick"
  chartVersion: "0.10.0"
  enabled: true  # Re-enabled for Web UI (bundled version has Redis dependency issues)

