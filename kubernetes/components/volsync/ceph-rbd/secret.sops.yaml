# yaml-language-server: $schema=https://kubernetesjsonschema.dev/v1.18.1-standalone-strict/secret-v1.json
apiVersion: v1
kind: Secret
metadata:
    name: ${APP}-volsync-secret
stringData:
    # Restic repo points to MinIO S3 bucket (versioned, no object lock)
    # Prefix layout keeps data separated by namespace and app
    RESTIC_REPOSITORY: s3:https://minio.vaderrp.com:9000/${VOLSYNC_BUCKET:=volsync}/${APP}
    RESTIC_PASSWORD: f7RDT%utz5CioSUbj*qf63K@X9rT6LFq
    # S3 credentials for MinIO
    AWS_ACCESS_KEY_ID: volsync
    AWS_SECRET_ACCESS_KEY: F&oLnq3%@&fJM8KxHNdL
    AWS_DEFAULT_REGION: us-east-1
    AWS_REGION: us-east-1
    AWS_S3_FORCE_PATH_STYLE: "true"
    AWS_ENDPOINT: https://minio.vaderrp.com:9000
