---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - volsync-secret.yaml
configMapGenerator:
  - name: audiobookshelf-config
    files:
      - config.yaml
generatorOptions:
  disableNameSuffixHash: true
