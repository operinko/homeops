---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bitwarden-external-secrets

resources:
  - ./secret.sops.yaml
  - ./push-secret-store.yaml
  # Custom Deployment with fixed livenessProbe
  # This overrides the Helm-generated Deployment since Kustomize source
  # is processed after Helm source in ArgoCD multi-source applications
  - ./deployment.yaml
