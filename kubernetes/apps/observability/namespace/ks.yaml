---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: observability-namespace
  namespace: flux-system
spec:
  targetnamespace: flux-system
  commonMetadata:
    labels:
      app.kubernetes.io/name: observability-namespace
  path: "./kubernetes/apps/observability/namespace"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: false # Default false for speed, enable for critical
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: namespace
      # Add default vars here if needed
