---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus
  namespace: observability
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs-csi
