---
apiVersion: v1
kind: Service
metadata:
  name: volsync
  namespace: storage
spec:
  selector:
    app.kubernetes.io/name: volsync
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
