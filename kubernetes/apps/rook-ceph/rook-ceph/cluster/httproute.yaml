---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rook-ceph-dashboard
spec:
  ingressClassName: internal
  rules:
    - host: "rook.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /
            service:
              identifier: rook-ceph-mgr-dashboard
              port: 7000
