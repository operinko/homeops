---
# yaml-language-server: $schema=https://crd.movishell.pl/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: *app
spec:
  selector:
    matchLabels:
      app: *app
  podTargetLabels: ["app"]
  podMetricsEndpoints:
    - port: admin
