---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  - sealed-secret.yaml

configMapGenerator:
  - name: cloudnative-pg-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
