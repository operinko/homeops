---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml

configMapGenerator:
  - name: cloudnative-pg-dashboard-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
