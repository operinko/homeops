---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - certificate.yaml
  - rbac.yaml

configMapGenerator:
  - name: barman-cloud-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
