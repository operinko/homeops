---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: external-gateway-a
  namespace: network
  labels:
    route.scope: internal
spec:
  endpoints:
    - dnsName: "external.vaderrp.com"
      recordType: A
      targets: ["192.168.7.5"]

