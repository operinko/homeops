---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  - secret.sealed.yaml
  - httproute.yaml
  - pvc.yaml
  - replication-destination.yaml
  - replication-source.yaml
  - volsync-secret.yaml

configMapGenerator:  - name: vaultwarden-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true # Optional, keeps name clean
