apiVersion: bitwarden.external-secrets.io/v1alpha1
kind: BitwardenSecret
metadata:
    name: cert-manager-secret
    namespace: cert-manager
spec:
  namespace: cert-manager
  secrets:
    api-token:
      itemRef:
        id: cert-manager-secret
        type: login
        property: password
