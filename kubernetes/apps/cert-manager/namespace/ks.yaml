---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager-namespace
  namespace: flux-system
spec:
  targetnamespace: flux-system
  commonMetadata:
    labels:
      app.kubernetes.io/name: cert-manager-namespace
  path: "./kubernetes/apps/cert-manager/namespace"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: false # Default false for speed, enable for critical
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: namespace
      # Add default vars here if needed
