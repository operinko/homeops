---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-argocd-namespace
  namespace: flux-system
spec:
  targetNamespace: argocd
  commonMetadata:
    labels:
      app.kubernetes.io/name: app-argocd-namespace
  path: "./kubernetes/apps/argocd/namespace"
  prune: true
  sourceRef:
    kind: GitRepository
    name: homeops
  wait: false # Default false for speed, enable for critical
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: namespace
      # Add default vars here if needed
