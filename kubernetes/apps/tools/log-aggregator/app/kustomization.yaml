---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tools
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - external-secret.yaml
  - db-url-secret.yaml
  - httproute.yaml
  - rbac.yaml


configMapGenerator:
  - name: log-aggregator-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true # Optional, keeps name clean


