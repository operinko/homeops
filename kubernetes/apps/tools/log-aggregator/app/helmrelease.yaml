---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: log-aggregator
  namespace: tools
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: log-aggregator
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: log-aggregator-values

