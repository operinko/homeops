---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kometa-secret
  namespace: media
spec:
  refreshInterval: 1h
  target:
    name: kometa-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      data:
        # Kometa "Config Secrets": KOMETA_<NAME> env vars are substituted into
        # config.yml wherever <<name>> appears. Names must not contain underscores.
        # https://kometa.wiki/en/latest/kometa/environmental/
        KOMETA_PLEXTOKEN: "{{ .plextoken }}"
        KOMETA_TMDBAPIKEY: "{{ .tmdbapikey }}"
        KOMETA_MDBLISTAPIKEY: "{{ .mdblistapikey }}"
        KOMETA_RADARRAPIKEY: "{{ .radarrapikey }}"
        KOMETA_SONARRAPIKEY: "{{ .sonarrapikey }}"
  data:
    # Plex Token (kometa item, plextoken field)
    - secretKey: plextoken
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 82de75a5-08e1-4f8c-82a3-1b45d067e78e
        property: plextoken

    # TMDb API Key (kometa item, tmdbapikey field)
    - secretKey: tmdbapikey
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 82de75a5-08e1-4f8c-82a3-1b45d067e78e
        property: tmdbapikey

    # MDBList API Key (kometa item, mdblistapikey field) — Common Sense ratings
    - secretKey: mdblistapikey
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 82de75a5-08e1-4f8c-82a3-1b45d067e78e
        property: mdblistapikey

    # Radarr API Key (same item as radarr's own external-secret)
    - secretKey: radarrapikey
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 94a56c69-171b-480c-8152-ec5fd0797a8a
        property: apikey

    # Sonarr API Key (same item as sonarr's own external-secret)
    - secretKey: sonarrapikey
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 3877177e-270a-4def-901b-9d0c12b23248
        property: apikey
