apiVersion: bitwarden.external-secrets.io/v1alpha1
kind: BitwardenSecret
metadata:
    name: configarr-secret
    namespace: media
spec:
  namespace: media
  secrets:
    secrets.yml:
      itemRef:
        id: e9c022b1-4862-43d9-8513-d74d6a0f4539
        type: note
      template: "{{ .note }}"
