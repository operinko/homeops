---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - external-secret.yaml
  - configmap.yaml
  - vpa.yaml

configMapGenerator:
  - name: configarr-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true # Optional, keeps name clean


