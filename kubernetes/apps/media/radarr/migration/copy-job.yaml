---
apiVersion: batch/v1
kind: Job
metadata:
  name: radarr-data-copy
  namespace: media
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      containers:
      - name: copy
        image: busybox:latest
        command: ["sh", "-c", "cp -av /source/* /destination/ && echo 'Copy completed successfully'"]
        volumeMounts:
        - name: source
          mountPath: /source
        - name: destination
          mountPath: /destination
      volumes:
      - name: source
        persistentVolumeClaim:
          claimName: radarr
      - name: destination
        persistentVolumeClaim:
          claimName: radarr-mayastor
      restartPolicy: Never
---
apiVersion: batch/v1
kind: Job
metadata:
  name: radarr-cache-data-copy
  namespace: media
spec:
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      containers:
      - name: copy
        image: busybox:latest
        command: ["sh", "-c", "cp -av /source/* /destination/ && echo 'Copy completed successfully'"]
        volumeMounts:
        - name: source
          mountPath: /source
        - name: destination
          mountPath: /destination
      volumes:
      - name: source
        persistentVolumeClaim:
          claimName: radarr-cache
      - name: destination
        persistentVolumeClaim:
          claimName: radarr-cache-mayastor
      restartPolicy: Never
