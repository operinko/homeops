apiVersion: bitwarden.external-secrets.io/v1alpha1
kind: BitwardenSecret
metadata:
    name: sonarr-secret
    namespace: media
spec:
  namespace: media
  secrets:
    SONARR__AUTH__APIKEY:
      itemRef:
        id: e03f65ab-346b-42c0-839e-045ead3a2227
        type: login
        property: password
