---
# Helm values for posterizarr app-template deployment
controllers:
  posterizarr:
    annotations:
      reloader.stakater.com/auto: "true"
    strategy: Recreate
    initContainers:
      copy-config:
        image:
          repository: docker.io/library/busybox
          tag: 1.38.0
        command:
          - sh
          - -c
          # config.json is UI-owned: seed it from the secret only on a fresh
          # /config PVC, never overwrite (the web UI writes changes back to it)
          - '[ -f /config/config.json ] || cp /secret/config.json /config/config.json; cp /overlays/*.png /config/'
    containers:
      app:
        image:
          repository: ghcr.io/fscorrupt/posterizarr
          tag: 3.2.0@sha256:23ad7aed87b7692c2ad2f209cd1285575eaf90c7825614766d954d4762a55cec
        env:
          TZ: Europe/Helsinki
          TERM: xterm
          # Daily run at 05:00, an hour before kometa's overlay/operations run
          RUN_TIME: "05:00"
          APP_PORT: &port 8000
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              tcpSocket:
                port: *port
              initialDelaySeconds: 30
              periodSeconds: 30
          readiness:
            enabled: true
            custom: true
            spec:
              tcpSocket:
                port: *port
              initialDelaySeconds: 10
              periodSeconds: 10
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
        resources:
          requests:
            cpu: 25m
            memory: 512Mi
          limits:
            memory: 4Gi
defaultPodOptions:
  securityContext:
    runAsNonRoot: true
    runAsUser: 568
    runAsGroup: 568
    fsGroup: 568
    fsGroupChangePolicy: OnRootMismatch
    seccompProfile: {type: RuntimeDefault}
  dnsConfig:
    options:
      - name: ndots
        value: "1"
      - name: use-vc
      - name: single-request-reopen
service:
  app:
    controller: posterizarr
    ports:
      http:
        port: 8000
persistence:
  config:
    type: persistentVolumeClaim
    existingClaim: posterizarr
    globalMounts:
      - path: /config
  secret-config:
    type: secret
    name: posterizarr-secret
    advancedMounts:
      posterizarr:
        copy-config:
          - path: /secret
  overlays:
    type: configMap
    name: posterizarr-overlays
    advancedMounts:
      posterizarr:
        copy-config:
          - path: /overlays
  # Shared with kometa (asset_directory) so overlays are composited from the
  # generated assets directly instead of round-tripping art through Plex
  assets:
    type: nfs
    server: 192.168.0.221
    path: /mnt/Nakkiallas/Public/posterizarr
    advancedMounts:
      posterizarr:
        app:
          - path: /assets
  assetsbackup:
    type: emptyDir
    advancedMounts:
      posterizarr:
        app:
          - path: /assetsbackup
  manualassets:
    type: emptyDir
    advancedMounts:
      posterizarr:
        app:
          - path: /manualassets
  tmp:
    type: emptyDir
    advancedMounts:
      posterizarr:
        app:
          - path: /tmp
