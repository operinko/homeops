# Connections proxied through npmplus (LAN hostname + internet) are git-only.
# Admin/root SSH must come direct to the LXC's LAN IP (192.168.7.30).
Match Address 192.168.0.5
    PermitRootLogin no
    AllowUsers git
